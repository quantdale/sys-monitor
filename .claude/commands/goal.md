Resume this repository's active development goal. Read repository instructions, `.agent/PLANNER_HANDOFF.md`, `.agent/EXECUTION_PROMPT.md` if present, and native goal/campaign/state/OpenSpec files. Reconcile with current Git/implementation. Resume an ACTIVE planner prompt from the first genuinely incomplete requirement through validation, state updates, commits, and pushes; otherwise fall back to native continuation semantics. If neither exists, report that a planner pass is required. Preserve stricter local rules and do not finish with known Critical/High regressions.